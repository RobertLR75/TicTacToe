# C# Namespace Conventions

Root namespace: `GameService`. All projects start from their project name, not the solution name.

Namespaces mirror folder structure:
- `GameService/` → `namespace GameService;`
- `GameService/Models/` → `namespace GameService.Models;`
- `GameService/Services/` → `namespace GameService.Services;`