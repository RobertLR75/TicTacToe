# References

No existing code references. This is a greenfield frontend implementation.

The backend `src/Backend/GameService/` class library exists but is not used by the frontend — the frontend implements its own game logic in `TicTacToeUI.Services.GameService`.
